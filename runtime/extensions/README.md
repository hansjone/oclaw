# extensions（本地重写版）

本目录用于承接 `vendor/oclaw/extensions` 的本地可维护说明，采用中文为主。

## 目录
- `api/`：扩展层 API 总览与接入约定。
- `whatsapp/`：WhatsApp 扩展能力与 API 清单（中文重写）。
- `webhooks/`：Webhooks 扩展能力与 API 清单（中文重写）。

## 目标
- 让团队快速理解扩展层做什么、怎么配、怎么排障。
- 避免直接在 vendor 目录改动第三方源码。
- 保持“可读文档在本地、源码在 vendor”的边界。
